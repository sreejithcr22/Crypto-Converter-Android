package com.codit.cryptoconverter.listener;

import androidx.fragment.app.Fragment;

public interface FragmentTransactionListener {
    void onFragmentTransaction(String fragmentTag, Fragment fragment);
}
